import itemC from './items/c'
// import _itemC from './items/_c'

const configs = [
  {
    field: 'multiArray',
    label: '二维数组',
    type: Array,
    itemType: Array,
    itemConfigs: itemC
    // _itemConfigs: _itemC
    // _parentsInfo: [{ label: null, field: null, type: Object, layer: 0 }]
  }
]

export default configs
